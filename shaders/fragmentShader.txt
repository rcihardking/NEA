#version 420 core
in vec2 UV;
in vec3 outNor;

out vec3 FragColor;

uniform sampler2D image;
  
void main()
{
    //FragColor = vec4(0.5f, 0.5f, 0.5f, 1.0f);
    vec3 light = 0.6 * vec3(1.0f, 1.0f, 1.0f);

    FragColor = light * texture(image, UV).rgb;
}